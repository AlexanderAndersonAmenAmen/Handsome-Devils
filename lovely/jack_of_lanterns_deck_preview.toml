[manifest]
version = "1.0.0"
dump_lua = true
priority = 25

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
local rank_name_mapping = {}
'''
position = "before"
payload = '''
if HNDS and HNDS.prepare_abstract_full_preview_suit_map then
    HNDS.prepare_abstract_full_preview_suit_map(suit_map)
end
'''
match_indent = true
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	for _, suit in ipairs(suit_map) do
		if suit_counts[suit] == 0 and SMODS.Suits[suit].in_pool and not SMODS.add_to_pool(SMODS.Suits[suit],{rank=''}) then
			hidden_suits[suit] = true
		end
	end
'''
position = "after"
payload = '''
	if HNDS and HNDS.hide_unused_abstract_preview_suits then
		HNDS.hide_unused_abstract_preview_suits(hidden_suits, suit_counts)
	end
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	for j = 1, #visible_suit do
		if (j >= 1 and j <= 4) or num_suits <= 4 then
'''
position = "at"
payload = '''
	local hnds_suits_per_page = (HNDS and HNDS.prepare_jol_deck_preview and HNDS.prepare_jol_deck_preview(visible_suit)) or 4
	local hnds_abstract_view = HNDS and HNDS.is_abstract_deck and HNDS.is_abstract_deck() or false
	local hnds_abstract_config_view = HNDS and HNDS.is_abstract_config_view_active and HNDS.is_abstract_config_view_active(visible_suit) or false
	for j = 1, #visible_suit do
		local hnds_show_suit
		if HNDS and HNDS.abstract_view_should_render_initial_suit then
			hnds_show_suit = HNDS.abstract_view_should_render_initial_suit(visible_suit[j], j, num_suits, hnds_suits_per_page, visible_suit)
		else
			hnds_show_suit = (j >= 1 and j <= hnds_suits_per_page) or num_suits <= hnds_suits_per_page
		end
		if hnds_show_suit then
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	if not next(deck_tables) then
		local view_deck = CardArea(
'''
position = "before"
payload = '''
	if hnds_abstract_view and HNDS and HNDS.append_abstract_view_deck_rows then
		HNDS.append_abstract_view_deck_rows(deck_tables, SUITS, unplayed_only or view_deck_unplayed_only)
	elseif hnds_abstract_config_page and HNDS and HNDS.append_abstract_config_view_deck_rows then
		HNDS.append_abstract_config_view_deck_rows(deck_tables, SUITS, unplayed_only or view_deck_unplayed_only, visible_suit, args and args.cycle_config and args.cycle_config.current_option)
	end
'''
match_indent = false
times = 2

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	local suits_per_row = 2
'''
position = "at"
payload = '''
	local suits_per_row = hnds_abstract_view and 5 or 2
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	while i <= math.min(4, #visible_suit) do
		if not hidden_suits[visible_suit[i]] then
'''
position = "at"
payload = '''
	local hnds_initial_suit_limit = hnds_abstract_view and #visible_suit or (hnds_abstract_config_view and #visible_suit or math.min(hnds_suits_per_page, #visible_suit))
	while i <= hnds_initial_suit_limit do
		if not hidden_suits[visible_suit[i]] and not (hnds_abstract_config_view and HNDS and HNDS.is_abstract_suit_key and HNDS.is_abstract_suit_key(visible_suit[i])) then
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''#visible_suit > 4 and {n = G.UIT.R'''
position = "at"
payload = '''((HNDS and HNDS.abstract_view_show_page_cycle and HNDS.abstract_view_show_page_cycle(visible_suit, hnds_suits_per_page)) or (not HNDS and #visible_suit > hnds_suits_per_page)) and {n = G.UIT.R'''
match_indent = true
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	local deck_start_index = (args.cycle_config.current_option - 1) * 4 + 1
	local deck_end_index = math.min(deck_start_index + 4 - 1, #visible_suit)
'''
position = "at"
payload = '''
	local hnds_suits_per_page = (HNDS and HNDS.prepare_jol_deck_preview and HNDS.prepare_jol_deck_preview(visible_suit)) or 4
	local hnds_abstract_config_view = HNDS and HNDS.is_abstract_config_view_active and HNDS.is_abstract_config_view_active(visible_suit) or false
	local hnds_abstract_config_page = HNDS and HNDS.abstract_config_page_two and HNDS.abstract_config_page_two(visible_suit, args.cycle_config.current_option) or false
	local deck_start_index = (args.cycle_config.current_option - 1) * hnds_suits_per_page + 1
	local deck_end_index = math.min(deck_start_index + hnds_suits_per_page - 1, #visible_suit)
	if hnds_abstract_config_view then
		if hnds_abstract_config_page then
			deck_start_index, deck_end_index = 1, 0
		else
			deck_start_index, deck_end_index = 1, #visible_suit
		end
	end
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
		if SUITS[visible_suit[j]][1] and (j >= deck_start_index and j <= deck_end_index) then
'''
position = "at"
payload = '''
		if SUITS[visible_suit[j]][1] and (j >= deck_start_index and j <= deck_end_index)
			and not (hnds_abstract_config_view and HNDS and HNDS.is_abstract_suit_key and HNDS.is_abstract_suit_key(visible_suit[j])) then
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	for _, suit in ipairs(suit_map) do
		if suit_tallies[suit] == 0 and SMODS.Suits[suit].in_pool and not SMODS.add_to_pool(SMODS.Suits[suit], {rank=''}) then
			hidden_suits[suit] = true
		end
	end
'''
position = "after"
payload = '''
	if HNDS and HNDS.hide_jol_suit_tally then HNDS.hide_jol_suit_tally(hidden_suits) end
	if HNDS and HNDS.hide_unused_abstract_preview_suits then HNDS.hide_unused_abstract_preview_suits(hidden_suits, suit_tallies) end
'''
match_indent = false
times = 2

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
			if v.base.value and not v_nr then rank_tallies[v.base.value] = rank_tallies[v.base.value] + 1 end
			if v.base.value and not v_nr and not v.debuff then mod_rank_tallies[v.base.value] = mod_rank_tallies[v.base.value] + 1 end
'''
position = "at"
payload = '''
			local hnds_preview_rank = (HNDS and HNDS.jol_preview_rank_key and HNDS.jol_preview_rank_key(v)) or v.base.value
			if hnds_preview_rank and not v_nr then rank_tallies[hnds_preview_rank] = (rank_tallies[hnds_preview_rank] or 0) + 1 end
			if hnds_preview_rank and not v_nr and not v.debuff then mod_rank_tallies[hnds_preview_rank] = (mod_rank_tallies[hnds_preview_rank] or 0) + 1 end
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
			if v.base.value then rank_tallies[v.base.value] = rank_tallies[v.base.value] + 1 end
			if v.base.value and not v.debuff then mod_rank_tallies[v.base.value] = mod_rank_tallies[v.base.value] + 1 end
'''
position = "at"
payload = '''
			local hnds_preview_rank = (HNDS and HNDS.jol_preview_rank_key and HNDS.jol_preview_rank_key(v)) or v.base.value
			if hnds_preview_rank then rank_tallies[hnds_preview_rank] = (rank_tallies[hnds_preview_rank] or 0) + 1 end
			if hnds_preview_rank and not v.debuff then mod_rank_tallies[hnds_preview_rank] = (mod_rank_tallies[hnds_preview_rank] or 0) + 1 end
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	for j = 1, #suit_map do
		if SUITS[suit_map[j]][1] then
			table.insert(visible_suit, suit_map[j])
		end
	end
'''
position = "after"
payload = '''
	if HNDS and HNDS.prepare_abstract_view_suit_order then
		HNDS.prepare_abstract_view_suit_order(visible_suit)
	end
'''
match_indent = false
times = 2

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
		if #n_nodes == suits_per_row then
'''
position = "at"
payload = '''
		if #n_nodes == (hnds_abstract_view and (#temp_list == 0 and 4 or #temp_list == 1 and 5 or 4) or suits_per_row) then
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
G.FUNCS.your_suits_page = function(args)
	if not args or not args.cycle_config then return end
'''
position = "at"
payload = '''
G.FUNCS.your_suits_page = function(args)
	if not args or not args.cycle_config then return end
	if HNDS and HNDS.is_abstract_deck and HNDS.is_abstract_deck() then return end
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	while i <= deck_end_index do
		if not hidden_suits[visible_suit[i]] then
'''
position = "at"
payload = '''
	while i <= deck_end_index do
		if not hidden_suits[visible_suit[i]] and not (hnds_abstract_config_view and HNDS and HNDS.is_abstract_suit_key and HNDS.is_abstract_suit_key(visible_suit[i])) then
'''
match_indent = false
times = 1

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	for _, v in ipairs(temp_list) do
		local n = {n = G.UIT.R, config = {align = "cm", minh = 0.05, padding = 0.05}, nodes = v}
		table.insert(tally_ui, n)
	end

	local suit_options = {}
'''
position = "at"
payload = '''
	for _, v in ipairs(temp_list) do
		local n = {n = G.UIT.R, config = {align = "cm", minh = 0.05, padding = 0.05}, nodes = v}
		table.insert(tally_ui, n)
	end
	if hnds_abstract_config_page and HNDS and HNDS.append_abstract_config_tally_rows then
		HNDS.append_abstract_config_tally_rows(tally_ui, suit_tallies, mod_suit_tallies, flip_col, visible_suit, args.cycle_config.current_option)
	end

	local suit_options = {}
'''
match_indent = false
times = 2

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
	for i = 1, math.ceil(#visible_suit / 4) do
'''
position = "at"
payload = '''
	local hnds_page_count = (HNDS and HNDS.abstract_view_page_count and HNDS.abstract_view_page_count(visible_suit, hnds_suits_per_page)) or math.ceil(#visible_suit / hnds_suits_per_page)
	for i = 1, hnds_page_count do
'''
match_indent = false
times = 2

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
		table.insert(suit_options,
			localize('k_page') .. ' ' .. tostring(i) .. '/' .. tostring(math.ceil(#visible_suit / 4)))
'''
position = "at"
payload = '''
		table.insert(suit_options,
			localize('k_page') .. ' ' .. tostring(i) .. '/' .. tostring(hnds_page_count))
'''
match_indent = false
times = 2

[[patches]]
[patches.pattern]
target = '=[SMODS _ "src/overrides.lua"]'
pattern = '''
			if v:is_suit(kk) then mod_suit_counts[kk] = mod_suit_counts[kk] + 1 end
'''
position = "at"
payload = '''
			local hnds_effective_suit
			if HNDS and HNDS.abstract_deck_preview_is_suit then
				hnds_effective_suit = HNDS.abstract_deck_preview_is_suit(v, kk)
			else
				hnds_effective_suit = v:is_suit(kk)
			end
			if hnds_effective_suit then mod_suit_counts[kk] = mod_suit_counts[kk] + 1 end
'''
match_indent = false
times = 1
